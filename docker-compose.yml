services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: sampahkupilah:web
    env_file:
      - .env
    environment:
      - NODE_ENV=production
    ports:
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/usr/src/app/data
    restart: unless-stopped
